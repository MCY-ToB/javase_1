package heap;

public class HeapSort {

    //构建堆
    public void initHeap(int[] arr){
        for (int i = 0; i < arr.length; i++) {
            int curIndex = i;
            int parentIndex = (i-1)/2;
        }
    }
}
